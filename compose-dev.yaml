services:
  app:
    entrypoint:
    - sleep
    - infinity
    image: adoptopenjdk/maven-openjdk13
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

