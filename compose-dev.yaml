services:
  app:
    entrypoint:
    - sleep
    - infinity
    image: adoptopenjdk/maven-openjdk8
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

